from collections import deque
import sys
input = sys.stdin.readline

# def bfs(v):

# def dfs(v):

n = int(input())
graph = [[0] * (n+1) for _ in range(n+1)]
visited_dfs = [0] * (n+1)
print(graph)
print(visited_dfs)
