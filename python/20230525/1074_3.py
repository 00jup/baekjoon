import sys
input = sys.stdin.readline

N, r, c = map(int, input().split())
count = 0


def recursion(size, x, y):
    global count
    if N==2:
      for i in range(2):
        for j in range(2):
          count += 1
          if 

recursion(n, 0, 0)
